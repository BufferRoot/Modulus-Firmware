# Evolver daily log — 2026-07-03

MEMORY.md sync from current workspace (no git commits yet — full untracked tree).

## Session wins

- **ABI epoch 18** — `settings_dump_copy` avoids double 8 KiB stack on taskLVGL; serial RX cap 2048 B
- **Multi-protocol CNC** — `protocol_engine.zig` + `linuxcnc/` + `mach3/` trees wired to `cnc_proto` NVS
- **Host tests 175** — `zig build test` green (afternoon re-check)
- **UI maintenance splits** — per-tab settings, status bar, power menu, wireless settings, DRO build; **102** C shims in CMake
- **Accent palettes** — `gen_ui_palettes.py` → 9 M3 schemes in `ui_palette_schemes.h`
- **CBM** — repo indexed (`fast` mode) afternoon

## Build baseline (workspace, afternoon)

| Artifact | Value |
|----------|-------|
| P4 bin | `0x2aa4c0` (~53% of 5 MB factory) |
| P4 ELF SHA256 | `ad4f51516c4aa82cb3e1e857e2359760517e1653597228b63d07a49395f547be` |
| ABI epoch | 18 |
| Host tests | 175 pass |
| C6 workspace build | `network_adapter` **0x192590** |
| C6 last dual flash (field) | 2.11.4 **0x182440** (2026-06-20) |

## Key files touched since 2026-06-20 memory

| Area | Files |
|------|-------|
| ABI | `src/modulus/firmware/abi.zig`, `abi_guard.zig` |
| CNC protocols | `src/modulus/cnc/protocol_engine.zig`, `linuxcnc/*`, `mach3/*` |
| ExtEncoder | `ext_encoder_state.zig`, `ext_encoder_poll_ops.zig`, `ext_encoder_jog.zig`, … |
| UI theme | `scripts/gen_ui_palettes.py`, `ui_palette_schemes.h`, `ui_theme.c` |
| Wireless | `wireless_shim.c` (1052L core), `wireless_shim_espnow.c`, `wireless_shim_802154.c` |
| New shims | `touch_shim.c`, `estop_gpio_shim.c`, `cnc_trace_shim.c` |

## Ops notes

- Reflash **partition table** if device still shows 3 MB factory (`0x300000`) — `partitions.csv` is 5 MB
- Close COM5 monitor before flash (`Access is denied`)
- C6 workspace bin **0x192590** ≠ last field flash **0x182440** — dual-flash if slave code changed
- LinuxCNC/Mach3: field handshake verify still open (grblHAL + ESP-NOW path field-verified 2026-06-20)

## Next

- Field: WiFi STA, RS-485 grblHAL motion, ESP-NOW grblHAL E2E over S3 bridge
- Field: LinuxCNC/Mach3 connect + status on RS-485 or WS
- Field: BMI270 cold-wake, PIN/deep-sleep walk
- Optional: split remaining `wireless_shim.c` (1052L); bump `docs/verify/Zig-Port-Checklist.md` ABI 14 → 18
