# Evolver daily log — 2026-07-04

MEMORY.md sync from current workspace (git still untracked — no commits).

## Session wins

- **Host tests 175** — `zig build test --summary all` green
- **Wireless shim trim** — `wireless_shim.c` **953L** (was 1052L)
- **C6 policy documented** — all C6 changes via `build_tab5_c6_modulus.ps1 -ZigbeeExclusive`; full fix catalog in MEMORY § C6 slave
- **Evolver** — `evolver run` cycle completed

## C6 canonical build (MUST)

```powershell
.\scripts\build_tab5_c6_modulus.ps1 -ZigbeeExclusive -Action build
.\scripts\build_tab5_c6_modulus.ps1 -ZigbeeExclusive -Action flash -Port COM6
# then P4: .\scripts\flash_tab5.ps1 -Port COM5
```

- **Edit only:** `firmware/tab5-c6/` — never `C:\modulus_tab5_c6_build\slave` mirror
- **`-ZigbeeExclusive`:** Thread off, ZBOSS hub in `zigbee_handler.c`, `partitions_zigbee.csv` (zb_storage/zb_fct)
- **`flash_tab5_dual.ps1`** passes **`-ZigbeeExclusive`** on C6 build/flash by default; `-NoZigbeeExclusive` for Thread MTD only

## C6 fixes captured (summary)

| Area | Fix |
|------|-----|
| SDIO 0x107 | Dual flash matching 2.11.4 modulus slave; GPIO15 reset must end HIGH (no strip patch) |
| ESP-NOW 0x01 | C6 local channel lock; disconnected-STA PM off; explicit peer.channel |
| ESP-NOW SDIO | Injected `espnow_handler.c` + `ESP_ESPNOW_IF` dispatch (upstream lacked it) |
| ESP-NOW ownership | `CONFIG_MODULUS_C6_ESPNOW=n` — SDIO handler owns single callback set |
| Zigbee hub | `-ZigbeeExclusive` + ZBOSS PATH C; zb_storage/zb_fct partitions; stale sdkconfig purge |
| Permit-join | `s_user_permit` closes auto BDB windows unless user tapped Pair |
| P4 transport | `espnow_transport_shim.c` always loads bridge MAC into `s_peer` before send |
| Zigbee/Thread RPC | Injected handlers + enum in `esp_hosted_interface.h` |
| IDF 6 SDIO | `hal/sdio_slave_periph.h` patch in mirror |

Full 20-row table: `MEMORY.md` § **C6 slave — canonical build & fix catalog**.

## Build baseline

| Artifact | Value |
|----------|-------|
| ABI epoch | 18 |
| Host tests | 175 pass |
| C6 workspace | **0x192590** (ZigbeeExclusive build) |
| C6 last field flash | **0x182440** (2026-06-20, pre-hub) |
| P4 last build | **0x2aa4c0** — no fresh bin in tree today |

## Next

- Dual-flash **0x192590** C6 with `-ZigbeeExclusive` then P4; power-cycle; verify `ZBOSS HUB PATH` + `Wireless ready`
- Field: Zigbee device join + OnOff; ESP-NOW grblHAL E2E; WiFi STA; RS-485 motion
