# Evolver daily log — 2026-06-20

Continues flash/build sessions from prior chat ([Flash C6 / P4](cb5ff7df-8acd-4293-a382-6726cd22243e)).

## Session wins

- **tab5-lib unblocked** — `tab5_root.zig` + Zig 0.16 freestanding std options; host tests unchanged on `root.zig`
- **P4 baseline** — `modulus_tab5.bin` `0x2a0c20`, ELF `e7c39a0d…`, ~47% factory free (5 MB partition)
- **C6 dual flash** — `network_adapter` 2.11.4 `0x182440` on COM6; `espnow_handler.c` use-before-declare fix
- **ui_shim.c** — LVGL guard uses `CONFIG_LV_USE_*` (sdkconfig.h)

## Files changed (2026-06-19/20)

| File | Change |
|------|--------|
| `src/modulus/tab5_root.zig` | **new** — freestanding root for tab5-lib |
| `build.zig` | tab5-lib → `tab5_root.zig`, `.single_threaded = true` |
| `src/modulus/core/monotonic_ms.zig` | `@divTrunc` for signed esp_timer divide |
| `src/modulus/hal/platform/idf_battery.zig` | `@enumFromInt` (replaces `intToEnum`) |
| `src/modulus/hal/platform/display.zig` | module-level `lockHw` / `unlockHw` / `applyTimeouts` |
| `src/modulus/cnc/deferred_connect.zig` | `fetchAdd` discard; `handle: *?*anyopaque` for xTaskCreate |
| `firmware/tab5/components/modulus_zig/ui_shim.c` | `CONFIG_LV_USE_CLIB_MALLOC` compile guard |
| `firmware/tab5-c6/main/espnow_handler.c` | `s_bcast_mac` + probe constants moved before use |

## Ops notes

- Close COM5 monitor before flash (`Access is denied` otherwise)
- Flash-only: `.\scripts\flash_tab5.ps1 -Port COM5 -SkipAscii -SkipBuild`
- Dual: `.\scripts\flash_tab5_dual.ps1 -C6Port COM6 -P4Port COM5`

## Next

- Cold-boot COM5: `Wireless ready` after dual flash + power-cycle
- Field: WiFi STA, RS-485 motion, ESP-NOW grblHAL over S3 bridge
